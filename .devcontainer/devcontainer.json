{
    "forwardPorts": [8000, 3001],
    "portsAttributes": {
        "8000": {
            "label": "Python Server",
            "onAutoForward": "notify"
        },
        "3001": {
            "label": "Live Preview",
            "onAutoForward": "silent"
        }
    },
    "postStartCommand": "gh codespace ports visibility 8000:public -c $CODESPACE_NAME || echo 'Auto-public failed'",
    "postCreateCommand": "cd ML_Backend && python3 -m venv venv && source venv/bin/activate && python3 -m pip install fastapi uvicorn scikit-learn pydantic"
}